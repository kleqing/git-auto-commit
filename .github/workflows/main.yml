name: Auto Commit

on:
  schedule:
  - cron: '0 0 * * *'

  workflow_dispatch:

jobs:
  commit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Auto commit script
        run: |
           git config --global user.email "hotaruu24@gmail.com"
           git config --global user.name "kleqing"
           git commit --allow-empty -m "New commit at: `date +'%Y-%m-%d %H:%M:%S'`" 
           git push origin main
